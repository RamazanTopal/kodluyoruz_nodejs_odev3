function circleArea(yaricap){
    console.log(Math.PI*yaricap*yaricap);
}

function circleCircumference(yaricap){
    console.log(2*Math.PI*yaricap);
}

module.exports ={
    circleArea,
    circleCircumference
}